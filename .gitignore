# Node.js / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json # 주석 처리하거나 필요에 따라 포함/제외
pnpm-lock.yaml # (만약 pnpm을 사용한다면)

# Cache directories
.cache/
**/node_modules/.cache

# Operating System files
.DS_Store
Thumbs.db 
# Environment files
.env
*.env
backend/.env  # 이 줄이나 유사한 패턴을 추가하세요
backend/.env.* # backend/.env/.1 같은 파일을 무시하기 위함

# Potentially sensitive text files (필요하다면)
# backend/Gai코드.txt # 만약 이 파일 전체를 올리지 않을 거라면